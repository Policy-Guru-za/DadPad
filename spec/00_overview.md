# Spec Overview

Use one numbered spec per non-trivial change.

One active spec at a time unless the user says otherwise.

| Spec | Title | Status | Owner | Depends On | Notes |
| --- | --- | --- | --- | --- | --- |
| `01_agents-build-loop` | Strengthen AGENTS with a spec-driven build loop | Done | Codex | None | Bootstrapped workflow docs, spec templates, and progress tracking |
| `02_dadpad-ipad-port` | Port PolishPad into DadPad with iPad-first UI and fresh DadPad storage | Done | Codex | `01_agents-build-loop` | Gate A closed; DadPad-native bundle/app builds, launches, and persists config on simulator |
| `03_ipad-polish-and-device-smoke` | Harden DadPad for iPad viewport fit, legibility, and device smoke | Done | Codex | `02_dadpad-ipad-port` | Senior-friendly shell hardening complete on simulator; physical iPad smoke deferred because no device was attached |
| `04_physical-ipad-smoke` | Recover Xcode device build path and prove DadPad on a connected iPad | Done | Codex | `03_ipad-polish-and-device-smoke` | Build/install path recovered; user manually verified the iPad app and reported only the clear-reset bug remaining |
| `05_clear-fresh-start-reset` | Make Clear restore DadPad to a true fresh-start state | Done | Codex | `04_physical-ipad-smoke` | Browser/sim reset path fixed; physical iPad follow-up found `window.confirm` still blocks entry into the clear path |
| `06_clear-confirmation-surface` | Replace JS confirm with an in-app clear confirmation flow that works on iPad | Superseded | Codex | `05_clear-fresh-start-reset` | Solved the unreliable `window.confirm` path, but the inline confirmation card caused layout scramble on the physical iPad |
| `07_clear-ui-reset-overlay` | Replace the inline clear UI with a stable bottom-sheet reset flow | Done | Codex | `06_clear-confirmation-surface` | User visually verified the physical-iPad build and confirmed the implementation is correct |
| `08_theme-preview-html` | Produce five standalone DadPad theme preview HTML files for palette selection | Done | Codex | `07_clear-ui-reset-overlay` | Five standalone theme previews delivered and browser-smoked at desktop plus iPad-like widths |
| `09_warm-sand-bottom-bar-reflow` | Adopt Warm Sand and remap DadPad into a 3x2 bottom action grid | Done | Codex | `08_theme-preview-html` | Warm Sand production refit landed; user review requested one final harmonization pass for readiness placement and button consistency |
| `10_status-chip-button-harmonization` | Move readiness to the hero and harmonize Warm Sand action styling | Done | Codex | `09_warm-sand-bottom-bar-reflow` | Hero readiness chip and harmonized bottom controls landed; follow-up requested for keyboard docking, footer cleanup, and brighter ready feedback |
| `11_keyboard-dock-and-surface-cleanup` | Dock the action bar above the keyboard and remove footer surface artifacts | Done | Codex | `10_status-chip-button-harmonization` | General dock/keyboard cleanup landed; physical iPad follow-up exposed a separate post-clear refocus regression plus a branded-logo integration request |
| `12_clear-reset-and-logo-lockup` | Stabilize post-clear reset behavior and replace the heading text with the DadPad logo lockup | Done | Codex | `11_keyboard-dock-and-surface-cleanup` | Clear reset and transparent hero logo landed; physical iPad review exposed a responsive-chip regression plus a home-screen icon refresh request |
| `13_chip-layout-and-ios-icon-refresh` | Restore the inline hero chip and refresh the iPad app icon | Done | Codex | `12_clear-reset-and-logo-lockup` | Inline hero chip and refreshed home-screen icon landed; next follow-up is to remove native landscape support entirely |
| `14_portrait-only-orientation-lock` | Lock DadPad to standard upright portrait orientation only | Done | Codex | `13_chip-layout-and-ios-icon-refresh` | Native iOS orientation declarations now advertise only upright portrait; user approved the prior UI/icon work and requested network gating next |
| `15_internet-availability-gate` | Gate DadPad behind internet reachability and recover automatically when online returns | Done | Codex | `14_portrait-only-orientation-lock` | Startup/wake connectivity probing, full-app offline overlay, and automatic recovery landed; user approved the direction and requested a cleaner single-surface editor next |
| `16_single-surface-editor` | Collapse the writing area into one visible editor card | Done | Codex | `15_internet-availability-gate` | Single-surface editor landed; user approved the cleaner writing area and requested a bottom-action remap next |
| `17_action-dock-remap` | Move Settings to the top row and place Copy beneath Clear | Done | Codex | `16_single-surface-editor` | Action dock remapped to `Polish / Clear / Settings` then `Share / Copy / spacer`; user approved the live iPad result and requested a voice-preserving `Polish` pass next |
| `18_voice-preserving-polish` | Recalibrate DadPad `Polish` to sound like the same person, just cleaner | Done | Codex | `17_action-dock-remap` | Voice-preserving `Polish` contract landed; live eval remains blocked only by missing local OpenAI config, and the next follow-up is Gmail-first email compose without disrupting generic share |
| `19_gmail-first-email-action` | Keep generic Share and add a Gmail-first compose button | Done | Codex | `18_voice-preserving-polish` | Generic Share stayed intact, a Gmail icon button landed in the dock, Gmail-first compose now preserves paragraph breaks, and the updated build was installed/launched on the connected iPad |
| `20_gmail-wordmark-button-refine` | Replace the Gmail button icon with the supplied Gmail wordmark | Done | Codex | `19_gmail-first-email-action` | The supplied transparent Gmail wordmark replaced the old envelope icon, the button fit was refined for breathing room, and the refreshed build was installed/launched on the connected iPad |
| `21_stronger-polish-sendability` | Make DadPad `Polish` more sendable, coherent, and assertive | Done | Codex | `20_gmail-wordmark-button-refine` | `Polish` no longer biases toward minimal edits, GPT-5 reasoning is bumped for `polish`, evals/tests now enforce capitalization/sendability harder, and only live API-backed output sampling remains blocked by missing local config |
| `22_clone-proven-polish-prompt` | Clone the proven PolishPad `Polish` prompt contract into DadPad | Done | Codex | `21_stronger-polish-sendability` | DadPad `Polish` now matches the prompt templates under `tmp/Prompt-templates`, GPT-5 reasoning controls are back to minimal, stale stronger-sendability eval assumptions were removed, and local gates are green |
| `23_notes-and-email-polish-split` | Split DadPad polish into notes and email actions | Superseded | Codex | `22_clone-proven-polish-prompt` | Superseded after user confirmed the standard `Polish` flow already covers notes, emails, and letters well enough on its own |
| `24_restore-single-polish` | Remove email polish and restore DadPad to a single `Polish` action | Done | Codex | `23_notes-and-email-polish-split` | Removed the email-only rewrite path, restored the single-button dock, and kept the proven `Polish` flow as the only transform action |
| `25_equal-size-dock-grid` | Reposition Notes and Gmail into equal-sized dock cells | Done | Codex | `24_restore-single-polish` | Notes now sits under `Polish`, Gmail under `Clear`, visible dock buttons share the same footprint, and the refreshed build was reinstalled on the physical iPad |
